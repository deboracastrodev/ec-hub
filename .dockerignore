# Keep dependencies installed by the Dockerfile in the image. Copying a host
# vendor directory would otherwise overwrite that cached Composer layer.
vendor/

# Build context noise
.git/
.env*
!.env.example
coverage.xml
coverage.txt
coverage/

# Node / Playwright (E2E) -- tooling de host, fora da imagem
node_modules/
test-results/
playwright-report/
blob-report/
